# 🚀 Быстрый старт ISS Telemetry Analyzer

## ⚡ Установка за 3 шага

### 1. Клонирование репозитория

```bash
git clone https://github.com/QuadDarv1ne/scientific-publications/projects/ISS_Telemetry_Analyzer.git
cd scientific-publications/projects/ISS_Telemetry_Analyzer
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Запуск

```bash
python main.py
```

Готово 🎉

---

## 📋 Основные команды

### Полный анализ (по умолчанию)

```bash
python main.py
```

**Выполняет:**
- ✅ Сбор орбитальных данных (5 минут)
- ✅ Расчет параметров орбиты
- ✅ Создание 3 графиков орбиты
- ✅ Симуляция условий среды (24 часа)
- ✅ Анализ радиации (180 дней)
- ✅ Прогноз видимости для 3 городов

**Время выполнения:** ~7-10 минут

---

### Быстрый режим

```bash
python main.py --quick
```

**Сокращенная версия:**
- Сбор данных: 2 минуты
- Симуляция: 12 часов
- Радиация: 30 дней

**Время выполнения:** ~3-5 минут

---

### Только орбитальный анализ

```bash
python main.py --orbital
```

**Включает:**
- Текущее положение МКС
- Сбор траектории
- Расчет параметров
- 3 графика орбиты

**Время выполнения:** ~5-7 минут

---

### Только анализ окружающей среды

```bash
python main.py --environment
```

**Включает:**
- Получение TLE данных
- Температурный профиль
- Радиационный анализ
- Комплексный отчет

**Время выполнения:** ~2-3 минуты

---

### Прогноз видимости для города

```bash
# Для Москвы
python main.py --visibility --city moscow

# Для Санкт-Петербурга
python main.py --visibility --city spb

# Для Новосибирска
python main.py --visibility --city novosibirsk
```

**Доступные города:**
- moscow (Москва)
- spb (Санкт-Петербург)
- novosibirsk (Новосибирск)
- ekaterinburg (Екатеринбург)
- kazan (Казань)
- sochi (Сочи)
- vladivostok (Владивосток)
- minsk (Минск)
- kiev (Киев)
- almaty (Алматы)

---

### Без отображения графиков

```bash
python main.py --no-plots
```

Полезно для:
- Работы на сервере без GUI
- Автоматизированного запуска
- Генерации только данных

---

## 📂 Где найти результаты

После выполнения все результаты сохраняются в папке `results/`:

```
results/
├── plots/                          # Графики и визуализации
│   ├── iss_ground_track.png       # Трек на карте
│   ├── iss_3d_orbit.png           # 3D орбита
│   ├── iss_velocity_profile.png   # Профиль скорости
│   ├── iss_environmental_conditions.png
│   └── iss_cumulative_radiation.png
│
├── reports/                        # Текстовые отчеты
│   └── telemetry_report_*.json    # JSON отчеты
│
└── analysis/                       # Данные анализа
    ├── orbital_parameters.json     # Параметры орбиты
    └── radiation_analysis.json     # Анализ радиации
```

---

## 🔧 Запуск отдельных модулей

### Орбитальный модуль напрямую

```bash
cd src
python iss_orbital_analysis.py
```

### Модуль окружающей среды напрямую

```bash
cd src
python iss_environment_analysis.py
```

---

## 🧪 Запуск тестов

### Все тесты

```bash
cd tests
python test_orbital.py
python test_environment.py
```

### Или с pytest

```bash
pytest tests/ -v
```

### С покрытием кода

```bash
pytest tests/ --cov=src --cov-report=html
```

---

## 📊 Примеры использования в Python

### Получить текущее положение МКС

```python
from src.iss_orbital_analysis import ISSTracker

tracker = ISSTracker()
position = tracker.get_current_position()

print(f"Широта: {position['latitude']}°")
print(f"Долгота: {position['longitude']}°")
```

### Собрать данные за 10 минут

```python
tracker = ISSTracker()
tracker.collect_positions(duration_minutes=10, interval_seconds=5)

# Рассчитать параметры
params = tracker.calculate_orbital_parameters()
print(f"Скорость: {params['avg_speed_kmh']:.0f} км/ч")
```

### Прогноз видимости

```python
from src.iss_orbital_analysis import predict_passes

# Для вашего города (широта, долгота)
predict_passes(latitude=55.7558, longitude=37.6173, n_passes=5)
```

### Анализ радиации

```python
from src.iss_environment_analysis import ISSEnvironmentAnalyzer

analyzer = ISSEnvironmentAnalyzer()
total_dose = analyzer.analyze_radiation_exposure(days=30)
print(f"Доза за 30 дней: {total_dose:.2f} мЗв")
```

---

## ⚠️ Решение проблем

### Ошибка: "No module named 'requests'"

```bash
pip install requests
```

### Ошибка: "API request failed"

Проверьте подключение к интернету.

**API требует доступа к:**

- `http://api.open-notify.org/`
- `https://celestrak.org/`

### Ошибка: "matplotlib backend"

**Для работы без GUI:**

```bash
export MPLBACKEND=Agg
python main.py --no-plots
```

### Долгий сбор данных

Используйте быстрый режим:

```bash
python main.py --quick
```

---

## 💡 Советы

### Оптимальное время сбора данных

- **Минимум:** 2-3 минуты (несколько точек)
- **Рекомендуется:** 5-10 минут (хорошая статистика)
- **Максимум:** 90+ минут (полный виток)

### Интервал между запросами

- **Минимум:** 5 секунд (ограничение API)
- **Рекомендуется:** 10-30 секунд
- **Для длительного сбора:** 60 секунд

### Прогноз видимости

**МКС видна невооруженным глазом при:**

- ✅ Ясном небе
- ✅ Сумерках (30-90 минут после заката или до рассвета)
- ✅ МКС освещена солнцем, а вы в тени

---

## 📚 Дополнительные ресурсы

- **README.md** - Полная документация
- **docs/methodology.md** - Методология исследования
- **docs/api_reference.md** - Справка по API
- **docs/scientific_background.md** - Научная база

---

## 🤝 Поддержка

Нашли баг? Есть вопросы?

- 📧 Email: maksimqwe42@mail.ru
- 💬 Issues: https://github.com/QuadDarv1ne/scientific-publications/issues
- 📖 Wiki: https://github.com/QuadDarv1ne/scientific-publications/wiki

---

## ⭐ Приятного использования!

Если проект полезен, поставьте ⭐ на GitHub
