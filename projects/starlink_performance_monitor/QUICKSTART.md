# 🚀 Быстрый старт - Starlink Performance Monitor

Этот проект поддерживает **два режима работы**: командная строка (CLI) и веб-интерфейс.

---

## 📋 Первоначальная настройка

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Настройка конфигурации
```bash
# Копируем пример конфигурации
copy config.example.json config.json

# Редактируем config.json под свои нужды
```

### 3. Инициализация базы данных
```bash
python main.py db-setup
```

---

## 🖥️ Режим 1: Командная строка (CLI)

### Доступные команды

```bash
# Показать справку
python main.py -h
```

### 📊 Мониторинг производительности
```bash
# Базовый мониторинг
python main.py monitor

# Расширенный мониторинг с данными Starlink
python main.py enhanced-monitor
```

### 📈 Генерация отчетов
```bash
# Создать отчет о производительности
python main.py report

# С дополнительными параметрами
python main.py report --period weekly --format pdf
```

### 🤖 ML-анализ
```bash
# Запуск анализа машинного обучения
python main.py ml

# Обнаружение аномалий и прогнозирование
python main.py ml --analysis anomaly
```

### 🔔 Система оповещений
```bash
# Запуск системы оповещений
python main.py alerts

# Проверка настроек уведомлений
python main.py alerts --test
```

---

## 🌐 Режим 2: Веб-интерфейс

### Запуск веб-приложения

```bash
# Запуск на стандартном порту (8050)
python main.py web

# Запуск на кастомном порту
python main.py web --port 5000
```

### Доступ к веб-интерфейсу

После запуска откройте в браузере:
- **Локально**: http://127.0.0.1:8050
- **В локальной сети**: http://[IP-адрес]:8050

### 📱 Доступные страницы

- `/` - Главный дашборд
- `/dashboard` - Панель мониторинга
- `/performance` - История производительности
- `/alerts` - Оповещения и уведомления
- `/reports` - Генерация отчетов
- `/ml_analysis` - ML-анализ и прогнозирование
- `/settings` - Настройки системы

---

## 🔧 Дополнительные возможности

### Использование конфигурации
```bash
# Указать кастомный файл конфигурации
python main.py monitor --config my_config.json
python main.py web --config production_config.json
```

### Фоновый запуск
```bash
# Windows (PowerShell)
Start-Process python -ArgumentList "main.py web" -WindowStyle Hidden

# Linux/Mac
nohup python main.py web &
```

### Проверка статуса
```bash
# Проверить подключение к базе данных
python main.py db-setup

# Запустить тесты
python -m pytest tests/ -v
```

---

## 📊 Примеры использования

### Сценарий 1: Разовая проверка
```bash
# 1. Инициализация БД
python main.py db-setup

# 2. Запуск одного цикла мониторинга
python main.py monitor --once

# 3. Генерация отчета
python main.py report --period daily
```

### Сценарий 2: Постоянный мониторинг с веб-интерфейсом
```bash
# 1. Настройка БД
python main.py db-setup

# 2. Запуск веб-интерфейса (в одном терминале)
python main.py web

# 3. Запуск фонового мониторинга (в другом терминале)
python main.py monitor --interval 15
```

### Сценарий 3: ML-анализ и оповещения
```bash
# 1. Запуск системы оповещений
python main.py alerts

# 2. Запуск ML-анализа для обнаружения аномалий
python main.py ml --mode anomaly-detection

# 3. Просмотр результатов в веб-интерфейсе
python main.py web
```

---

## 🐛 Решение проблем

### Проблема: База данных не инициализирована
```bash
# Решение
python main.py db-setup
```

### Проблема: Порт уже занят
```bash
# Решение - использовать другой порт
python main.py web --port 8051
```

### Проблема: Ошибки импорта
```bash
# Решение - переустановить зависимости
pip install -r requirements.txt --upgrade
```

---

## 📚 Дополнительная документация

- Полная документация: [README.md](README.md)
- Docker: [DOCKER.md](DOCKER.md)
- Участие в разработке: [CONTRIBUTING.md](CONTRIBUTING.md)

---

## 💡 Полезные команды

```bash
# Показать версию Python
python --version

# Показать установленные пакеты
pip list

# Обновить все зависимости
pip install -r requirements.txt --upgrade

# Запустить все тесты
python -m pytest tests/ -v

# Очистить кэш Python
find . -type d -name __pycache__ -exec rm -rf {} +  # Linux/Mac
Get-ChildItem -Recurse -Directory -Filter __pycache__ | Remove-Item -Recurse -Force  # Windows
```

---

## 🎯 Рекомендации

1. **Для тестирования**: Используйте CLI режим для быстрой проверки
2. **Для продакшена**: Используйте веб-интерфейс для постоянного мониторинга
3. **Для анализа**: Комбинируйте оба режима - мониторинг в CLI, визуализация в Web
4. **Для автоматизации**: Настройте cron/задачи Windows для периодического запуска CLI команд

---

**Готово! Теперь вы можете использовать проект как через командную строку, так и через веб-интерфейс! 🎉**
